server:
  port: 9000

spring:
  application:
    name: fase6
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:simpledb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop